const findMyLocation  = () => {

    const status = document.querySelector('.status')

    const success = (postion) => {
        console.log(postion)
    }
    const error = "not able to get location"
}